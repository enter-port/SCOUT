seed: 42
# eval_can_shell_eta035.yaml -- 方案A dose bracket, upper point (user order
# 2026-08-27).  eval_can_shell.yaml (eta 0.115) measured mean_inject 0.30-0.40
# vs 方案三's calibrated ~1.0 (the killed eta-3.0 run's "26x" was partly
# off-manifold 1/sigma0^2 amplification, so the on-manifold shell gradient is
# ~9x 方案三's, not 26x).  Linear rescale to parity: 0.115 * (1.0/0.35)
# ~= 0.33; run eta 0.35.  Identical to eval_can_shell.yaml otherwise.
# Stale absolute paths below are ALWAYS overridden by run_rollout CLI.
base_dp:
  initial_ckpt_path: /root/workspace/baojiachun/scout/data/experiment2/can/train/DP/DP-base/checkpoints/580.ckpt
  config_name: base_dp_can_image
  config_dir: configs
vib:
  ckpt_path: /root/workspace/baojiachun/scout/data/experiment2/can/train/dyn/dyn-base/scout_vib.ckpt
  base_dp_ckpt: /root/workspace/baojiachun/scout/data/experiment2/can/train/DP/DP-base/checkpoints/580.ckpt
  view_names:
  - agentview_image
  - robot0_eye_in_hand_image
  proprio_dim: 9
  proprio_emb_dim: 64
  style_dim: 16
  hidden_dim: 128
dataset:
  path: /root/workspace/baojiachun/scout/data/experiment2/can/rollout/can_core.hdf5
  core_filter_key: core_20
action_dim: 10
eval:
  n_init_states: 100
  try_times: 5
  horizon: 300
  n_envs: 12
  log_every: 10
  view_names:
  - agentview_image
  - robot0_eye_in_hand_image
  proprio_keys:
  - robot0_eef_pos
  - robot0_eef_quat
  - robot0_gripper_qpos
exploration:
  guidance_scale: 0.35             # 2026-08-27: mean_inject parity bracket (~0.35*2.9=1.0)
  guidance_start_timestep: 100
self_improvement:
  num_rounds: 6
  scout_aug_mask: scout_aug
  num_epochs: 1500
wandb:
  use_wandb: true
  project: CAN-8-24-entropy
  tags:
  - formal
  - entropy
device: auto
log_dir: logs/scout_eval_can_image
explore:
  base_seed_round1: 1042
  n_scenes: 100
  try_times: 1
