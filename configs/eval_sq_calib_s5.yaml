seed: 42
# eval_square_entropy.yaml -- FORMAL entropy experiment, SQUARE (2026-08-26).
# Copy of eval_can_entropy.yaml with square fields (horizon 500,
# base_dp_square_image). guidance_scale 3.0 = PROVISIONAL (can's calibrated
# optimum); square dose-response runs first, final value patched (atomic
# write-new+mv) before round 1. Stale paths ALWAYS overridden by run_rollout
# CLI (--base-dp-ckpt / --vib-ckpt / --core-hdf5); dataset.path follows
# --core-hdf5 (run_rollout.py:197).
base_dp:
  initial_ckpt_path: /root/workspace/baojiachun/scout/data/2026_8_14/experiment1/square/train/DP/DP-base/checkpoints/580.ckpt
  config_name: base_dp_square_image
  config_dir: configs
vib:
  ckpt_path: /root/workspace/baojiachun/scout/data/2026_8_14/experiment3/square/train/dyn/dyn-SCOUT-exp4/20260819-201418/scout_vib.ckpt
  base_dp_ckpt: /root/workspace/baojiachun/scout/data/2026_8_14/experiment1/square/train/DP/DP-base/checkpoints/580.ckpt
  view_names: [agentview_image, robot0_eye_in_hand_image]
  proprio_dim: 9
  proprio_emb_dim: 64
  style_dim: 16
  hidden_dim: 128
dataset:
  path: /root/workspace/baojiachun/scout/data/robomimic/square/ph/image_v141_abs_core20.hdf5
  core_filter_key: core_20
action_dim: 10
eval:
  n_init_states: 100
  try_times: 5
  horizon: 500
  n_envs: 12
  log_every: 10
  view_names: [agentview_image, robot0_eye_in_hand_image]
  proprio_keys: [robot0_eef_pos, robot0_eef_quat, robot0_gripper_qpos]
exploration:
  guidance_scale: 5.0             # calib variant
  guidance_start_timestep: 100
self_improvement:
  num_rounds: 6
  scout_aug_mask: scout_aug
  num_epochs: 1500
wandb:
  use_wandb: true
  project: SQUARE-8-26-entropy
  tags: [formal, entropy]
device: auto
log_dir: logs/scout_eval_square_image
explore:
  base_seed_round1: 1042
  n_scenes: 100
  try_times: 1
